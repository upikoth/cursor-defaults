# Cursor defaults

Общие настройки Cursor для репозиториев (правила, AGENTS, skills). Подключаются как submodule в `.cursor`.

После выполнения git clone нужно переименовать папку `cursor-defaults` в проекте в `.cursor`
